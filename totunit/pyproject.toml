[project]
name = "tot-unit"
version = "0.1.0"
description = "A generic Tree-of-Thought SDK with interface-first design"
requires-python = ">=3.10"
dependencies = [
  "pydantic>=2.6",
  "openai>=1.40",
]

[tool.setuptools]
package-dir = {"" = "src"}


